# 달나루 앱 — 이어서 개발하기

## 실행 (3분)

```bash
npm install
npm run dev
```

→ http://localhost:3000 열면 됩니다. Node.js 20 이상 필요.

## 워크넷 실공고 켜기

1. data.go.kr에서 "워크넷 채용정보" 활용신청 (무료, 10분)
2. `.env.local.example`을 `.env.local`로 복사하고 발급받은 키 입력
3. 재시작하면 일자리 추천이 예시 → 실제 공고로 자동 전환

## 구조 한 눈에

| 경로 | 내용 |
| --- | --- |
| `app/page.tsx` | 안내데스크 (첫 화면 대화) |
| `app/funnel/` + `components/funnel/` | 이력서 대화 — 메인 제품 |
| `app/calc/` + `lib/pension.ts` | 은퇴 자금 계산기 (수치 출처 주석에) |
| `app/call/` | 전화 안내 상담원 |
| `app/guide/` + `lib/guideData.ts` | 도구 가이드 (질문·레시피는 데이터 배열 — 추가 = 배열에 항목 추가) |
| `app/gift/` | 부모님 이력서 선물하기 (링크 생성) |
| `lib/funnelCollect.ts` | **데이터 수집** — 모든 응답이 Firestore `funnel_sessions`로 |
| `app/api/jobs/` | 워크넷 프록시 (키 없으면 예시 폴백) |
| `public/tools.html` | WorkflowHub v7 (정적) |

## 원칙 (코드 주석에도 있음)

- 전면에 AI 용어 안 씀 (0806 회의 합의) · 가짜 수치 금지 (실측·출처만)
- 목록을 주지 말고 상황을 묻는다 · 보상 먼저, 심화 질문은 나중
- 이름은 저장 안 함 — 수집은 익명 (동의 문구는 퍼널 시작 게이트)

## 데이터 보기

Firebase 콘솔 → 프로젝트 `dalnaru` → Firestore → `funnel_sessions` 컬렉션.
`steps[]`의 q/a/tMs로 문항별 응답과 체류 시간이 쌓입니다.

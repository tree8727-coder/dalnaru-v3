# 달나루 프로젝트 핸드오버

## 2026-08-11 · 프로덕션 완주 테스트 — 아무도 못 보던 버그 2건 (Claude Code)

**배포는 했는데 아무도 프로덕션에서 대화를 끝까지 해본 적이 없었다.**
진짜 폰 크기(390×844)로 시작부터 이력서 완성까지 눌러봤다. 두 개가 나왔다.

**바꾼 것**

| 무엇 | 파일 | 한 줄 |
| --- | --- | --- |
| 봇 반복 발화 | `lib/funnelData.ts` | `style` 질문의 도입부 제거 (앞 ACK와 같은 문장이었음) |
| 이력서 비문 | `lib/funnelData.ts` | 연차 칩을 문장에 그대로 박던 5곳 → `YEARS_PHRASE` 한 곳 |
| 안 쓰는 인증 배선 | `lib/firebase.ts`, `app/layout.tsx`, `context/` 삭제 | 번들 **141KB(11%) 감소**, 콘솔 400 에러 제거 |
| 완주 테스트 | `scripts/e2e-funnel.mjs` (`npm run check:e2e`) | 배포 전에 돌린다 |
| 문장 검사 | `scripts/check-resume-text.mjs` (`npm run check:resume`) | 연차 네 선택지 문장 성립 여부 |

**왜**

1. **봇이 같은 칭찬을 두 번 했다.** `tacit` 맞장구가 "좋은 말씀입니다. 이력서에 꼭
   담겠습니다."를 말한 직후, 다음 질문이 같은 문장을 도입부로 또 말했다.
   경청·칭찬 톤이 이 제품의 차별점인데 반복되면 대본처럼 읽힌다.

2. **이력서에 "10년 이하 동안 건설/건축 현장을 지켜온" 이 찍혔다.** 연차 칩 라벨을
   문장에 그대로 끼워 넣고 있었다. 네 칩 중 둘이 비문이 됐고, 그중 하나
   (`30년 이상 평생` → "30년 이상 평생 동안")는 이 나이대가 제일 많이 고를 항목이다.
   **기업에 내는 문서라 어색한 한 줄이 그대로 사용자의 손해가 된다.**
   고칠 때 기준은 "부풀리지 않기"였다 — 3년 일한 사람의 `10년 이하`를 "10년 남짓"으로
   바꾸면 경력 과장이다. 그래서 `여러 해`. `30여 년`은 조금 줄여 말하지만 안전한 쪽이다.

3. **안 쓰는 인증을 걷어냈다.** `AuthProvider`는 v1 북마크 잔재로 쓰는 화면이 하나도
   없었는데, `lib/firebase.ts`가 최상단에서 `getAuth()`를 부르는 탓에 퍼널을 여는 것만으로
   `firebase/auth`가 딸려왔고 매 페이지 로드마다 요청이 400으로 떨어졌다.
   없는 기능의 값을 5060 사용자의 낡은 폰이 대신 치르고 있었다.

**버린 선택지** — 비문을 문장 템플릿 쪽에서 고치는 안. 값이 다섯 군데에 쓰이는데
템플릿마다 고치면 새 자리가 생길 때 또 샌다. 값 하나를 고쳐 다섯 군데가 같이 낫는 쪽을 골랐다.

**확인된 것** (프로덕션 실측, 2026-08-11)

- 완주 됨: 0% → 100% → 이력서 생성 → 회고 카드
- 콘솔 에러 0건 · 봇 반복 발화 0건 · 가로 넘침 없음(390/390)
- Firestore 쓰기 200 — **수집은 정상 동작 중**

**사람이 확인할 것**

1. **`lib/funnelData.ts`를 건드렸다.** `팀작업.md` 분담상 정회광 담당 파일이라
   같이 열면 충돌한다. 정회광이 작업 전 `git pull` 할 것.
2. **배포 전에 `npm run check:e2e`.** 빌드·타입체크는 이런 걸 안 잡아준다.
3. 회고 카드 접기/펴기 버튼이 대화 맨 끝에 있어 자동 테스트가 그 자리를 맴돈다.
   사람에게는 문제가 아니지만, 완주 이후 흐름이 더 있는지 한 번 봐줄 것.

## 2026-08-10 · 공개 배포 + DB 잠금 + 가이드 실측 정정 (Claude Code)

**앱이 공개됐다: https://dalnaru.vercel.app** (Vercel 프로덕션, `dalnaru` 프로젝트)

**바꾼 것**

| 무엇 | 파일 | 한 줄 |
| --- | --- | --- |
| Firestore 규칙 | `firestore.rules`, `firebase.json` | `funnel_sessions` 쓰기 전용. 읽기·삭제 차단 |
| 공유 카드(OG) | `app/layout.tsx`, `public/og.png`, `public/og-source.html` | 카톡·밴드 링크 미리보기 |
| 검색 노출 | `app/robots.ts`, `app/sitemap.ts` | 색인 경로 |
| 가이드 데이터 | `lib/guideData.ts` | 실패하는 명령 2건·부풀린 별 수 1건 정정 + 2종 추가 |
| 데이터 검사 | `scripts/check-guide.mjs` (`npm run check:guide`) | 설치 ID·별 수 실측 대조 |
| 칩 잘림 | `app/globals.css` | `calc-shell` 안에서 칩 상자 높이 뚜껑 해제 |

**왜**

1. **배포를 먼저 했다.** 로컬에만 있으면 트래픽 0이고, 트래픽 0이면 수집 데이터도 0이다.
   미리보기 배포는 버렸다 — 링크가 임시라 카톡으로 돌릴 수 없으면 배포한 의미가 없다.

2. **배포하자마자 DB가 전체 공개 읽기였다.** Firebase 웹 키는 클라이언트 번들에 그대로
   들어가므로, 공개 URL이 생긴 순간 누구나 남의 자유서술(성과 일화·전수 노하우)을
   내려받을 수 있는 상태였다. 규칙을 잠갔다. **팀 열람은 콘솔·Admin SDK가 규칙을 우회해
   그대로 된다.** `saveFunnel`이 실패를 localStorage로 삼키기 때문에 규칙 배포 후
   쓰기부터 확인했다 — 쓰기 200 / 읽기 403 / 스키마 밖 필드 403 실측.

3. **가이드는 "25종 확장" 대신 사실부터 고쳤다.** `winget install hledger`(패키지 없음),
   `npm i -g @mendable/firecrawl-cli`(npm 404), ImageMagick 별 4.4만(실제 1.7만)이
   라이브였다. 5060 대상에서 붙여넣은 명령이 실패하면 신뢰가 먼저 깨진다.
   tools.html 실데이터는 25종이 아니라 12종이고 그중 개발자용은 이미 뺀 항목이라,
   winget으로 확인되는 비개발자용 2종(Ollama·Pandoc)만 더했다.

**버린 선택지** — n8n(별 19.9만)·Stirling-PDF. 좋은 도구지만 winget 패키지가 없어
설치가 npm/Docker다. 같은 실패를 다시 만든다. `public/tools.html` 랭킹에는 그대로 있다.

**사람이 확인할 것**

1. **워크넷 키는 Vercel 환경변수에도 넣어야 한다.** `.env.local`은 로컬 전용이다.
   로컬에서 실공고가 떠도 배포본은 예시 공고를 계속 쓴다.
2. **`leads` 컬렉션은 이제 막혀 있다.** v1 모달 리드 수집(아래 옛 문서 3절)을 되살리면
   쓰기가 실패한다. 되살릴 거면 `firestore.rules`에 규칙을 먼저 추가할 것.
3. **가이드에 도구를 추가하기 전 `npm run check:guide`.** 통과 못 하면 넣지 않는다.
   화면의 "MS 검증" 배지는 모든 레시피가 winget 패키지라는 전제에 기대고 있다 —
   winget에 없는 도구를 넣는 순간 그 배지가 거짓이 된다.
4. **커스텀 도메인.** `*.vercel.app`은 기관 제휴 자료에 쓰기엔 임시 냄새가 난다.
5. OG 카드 문구를 고치려면 `public/og-source.html`을 열어 1200×630으로 다시 캡처한다.

## 2026-08-10 · 수집 동의 문구에 "통계 작성" 목적 명시 (Claude Code)

**바꾼 것** — 문구 3곳 + 로그 문자열 1곳. 로직·수집 항목은 안 건드림.

| 위치 | 전 | 후 |
| --- | --- | --- |
| `FrontDesk.tsx:138` | 응답은 **이름 없이** 보관됩니다 | 응답은 이름 없이, **통계·연구 목적으로** 보관됩니다 |
| `EntryFunnel.tsx` PROMISE | 이름을 뺀 **익명** 자료로 시니어 일자리 연구에 | 이름을 뺀 자료로 **통계 작성과** 시니어 일자리 연구에 |
| `EntryFunnel.tsx:528` funnel-privacy | 이름을 뺀 **익명** 자료로 저장되어 | 이름을 뺀 자료로 저장되어 **통계 작성과** |

**왜**

1. **"익명"은 사실과 다르다.** 세션ID + 분야·연차·역할·성과·목표 + 문항별 체류시간(`tMs`)이
   남는다. 익명정보는 재식별이 불가능해야 하는데 이건 그 수준이 아니다. 틀린 단어를 쓰면
   그 단어 자체가 나중에 문제가 된다. 그래서 "익명"을 뺐다.

2. **"통계 작성"을 넣은 이유.** 개인정보보호법 제28조의2는 가명정보를 통계작성·과학적연구·
   공익적기록보존 목적이면 정보주체 동의 없이 활용·제공할 수 있게 한다. 수집 시점에 그
   목적이 고지돼 있어야 나중에 B2B 데이터 제공 경로가 열린다. 지금 문구는 "연구"만 있었다.

3. **"가명정보"라는 법률 용어는 화면에 안 넣었다.** 5060 사용자가 못 알아듣는다.
   `시작하기.md`의 "전면에 AI 용어 안 씀"과 같은 이유다. 법적으로 핵심인 목적(통계 작성)만
   쉬운 말로 넣고, 용어는 향후 개인정보처리방침 문서에서 정확히 쓴다.

**버린 선택지** — 동의 체크박스를 필수/선택으로 분리하는 안. 지금 퍼널은 "시작하기" 버튼
하나로 진입하는 구조라 체크박스를 넣으면 이탈이 는다. 문구부터 바로잡고, 분리는 별건으로 본다.

**사람이 확인할 것**

1. **아직 안 된 것 — 묵시적 동의.** "시작하시면 동의하신 것으로 알겠습니다"는 명확한
   의사표시로 보기 어렵다. 동의 버튼과 시작 버튼이 같다.
2. **아직 안 된 것 — 제3자 제공 동의.** "활용"은 내부 활용이다. B2B로 넘기려면 별도 동의가
   필요하다. 다만 가명정보 + 통계·연구 목적이면 제28조의2로 동의 없이 가능한 경로가 있다.
3. **개인정보처리방침 페이지가 없다.** 문구에서 링크할 곳이 없는 상태다.


## 2026-08-08 · 디자인 토큰을 vault 브랜드로 교체 (이슈 #1, Claude Code)

**바꾼 것** — `app/globals.css`만. 색 외에는 안 건드림.

- `:root` 전체를 vault 브랜드 토큰으로 교체 (출처: `vault/AI-Sessions/wiki/design/brand-usage-content.md`)
- `.btn-primary` 글자색 `#fff` → `var(--bg-color)` — 옐로 면에 흰 글자는 1.5:1로 안 보임
- `pulse-ring` 애니메이션의 옛 파랑 rgba → 반달 옐로 rgba

**정한 값과 대비** (최악 배경인 card-hover 기준)

| 토큰 | 값 | 대비 |
| --- | --- | --- |
| `--bg-card` / `--bg-card-hover` | `#28356E` / `#313F7D` | 네이비 밝힘 1·2단 |
| `--text-secondary` | `#D5D0BC` | 6.35:1 ✅ |
| `--text-muted` | `#B9B4A0` | 4.72:1 ✅ (기존 `#636366`은 3.51:1 미달이었음) |
| 버튼 글자 (네이비 on 옐로) | | 10.95:1 ✅ |

`npm run build` 통과.

**사람이 확인할 것**

1. 실제 화면 느낌 — `npm run dev`로 열어볼 것 (네이비+크림이 5070에게 어떤지)
2. 컴포넌트 5개 파일에 **기존부터 박혀 있던 HEX 30곳** — 이번 범위 밖이라 안 건드림.
   토스 팔레트 잔재가 남아 있을 수 있음. 후속 이슈 후보
3. 브랜드 색이 아직 `draft`임 — 컨셉 재정의 후 바뀌면 이 파일 `:root`만 고치면 됨

---

## (이전) 최종 MVP 릴리스용 핸드오버

## 1. 프로젝트 개요 및 현재 상태
- **경로:** `C:\Users\tree0\.gemini\antigravity\scratch\dalnaru-v3`
- **스택:** Next.js (App Router), React, Vanilla CSS (Tailwind 없음), Firebase Firestore
- **목표:** 5060 반퇴자를 위한 프리미엄 은퇴 방어 파이프라인 플랫폼 "달나루" V30 MVP 런칭 (교수님 미팅용 최종본).

## 2. 교수님 미팅(최종본)을 위한 4대 핵심 과제 (Next Agent Action Items)

### 과제 A. 사용자 경험(UX) 기반 탭 순서 및 스토리텔링 재배치
사용자가 사이트에 들어왔을 때 설득되는 서사 구조로 `app/page.tsx`의 탭 순서를 재배치해야 합니다.
1. **은퇴 자산 진단기:** (Hook) 4가지 질문으로 소득 크레바스를 진단하여 심리적 니즈 자극.
2. **시니어 현직자 멘토링:** (Solution 1) 기존 경력을 살려 청년/스타트업을 돕고 수익 창출.
3. **5060 유망 자격증:** (Solution 2) 새로운 기술을 배워 재취업.
4. **AI 창업 도구:** (Solution 3) 소자본 지식 창업.
5. **AI 텔레마켓 비서:** (Retention) 일상생활의 편리함(예매, 스팸 차단 등)을 돕는 리텐션 도구.

### 과제 B. 시니어 멘토링 & AI 텔레마켓 앱 고도화 및 수익화 엣지 구현
단순한 나열식 설명이 아닌, 실제 작동하는 데모 앱(App) 수준의 UI로 고도화합니다. (`components/SeniorServices.tsx` 분리 또는 확장)
- **시니어 멘토링 엣지:** 단순 취업 상담이 아닌, "스타트업/청년 창업가"와 "대기업/전문직 은퇴자"를 매칭. 수익화: 매칭 수수료 및 프리미엄 구독 모델.
- **AI 텔레마켓 엣지:** 복잡한 모바일 앱이 아닌 "전화 한 통"으로 KTX 예매, 관공서 서류 발급 안내, 보이스피싱 판독을 해주는 음성 AI 컨시어지. 수익화: 통신사 B2B 제휴 또는 월 9,900원 구독제.
- *요구사항:* 두 서비스를 각각 독립된 컴포넌트로 만들고, 실제 앱을 시뮬레이션하는 화면(UI)을 구현하세요.

### 과제 C. AI 창업 도구 뷰 카테고리화 (기존 ai.html 복원)
- 현재 평면적으로 나열된 도구들을 원래 사용자가 원했던 것처럼 카테고리별(예: 마케팅, 디자인, 개발, 업무효율 등)로 깔끔하게 묶어서 보여주도록 UI를 개편하세요.

### 과제 D. 디자인 및 디테일 폴리싱
- 모바일과 PC 모두에서 완벽하게 보이도록 다크 모드 프리미엄 테마(`globals.css`)를 유지하면서 패딩/마진/색상을 교정하세요.
- 각 서비스별로 Firebase 모달 리드 수집 기능이 유지되도록 하세요.

## 3. Firebase 설정 정보 (유지 필요)
- `leads` 컬렉션에 데이터가 저장되고 있음. `lib/firebase.ts` 설정은 완료된 상태임.

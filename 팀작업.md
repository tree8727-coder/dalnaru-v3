# 둘이서 같이 고치는 법

충돌은 **같은 파일을 동시에 열어서** 생긴다. 그러니 파일을 나눈다.

## 파일 나누기

| 누가 | 무슨 파일 | 뭘 하나 |
| --- | --- | --- |
| **정회광** | `lib/` 안의 데이터 파일만 | 목록에 항목 추가·수정·삭제 |
| **유재원** | 그 외 전부 (`app/`, `components/`, `*.css`) | 화면·기능·배포 |

**정회광이 여는 파일은 이 셋뿐이다.**

| 파일 | 내용 |
| --- | --- |
| `lib/guideData.ts` | 도구 가이드 — 질문과 레시피 |
| `lib/funnelData.ts` | 대화 질문과 선택지 |
| `lib/certData.ts` | 자격증 목록 (⚠️ 아래 참고) |

`.tsx`로 끝나는 파일은 열지 않는다. 화면이 깨진다.

## 시작하기 전에 항상

```
git pull
```

이거 안 하고 고치면 부딪힌다. **작업 시작할 때 한 번만 치면 된다.**

## 다 고쳤으면

```
npm run build
```

통과하면 올린다. 실패하면 뭔가 깨진 거니 그대로 두고 물어본다.

## 데이터 파일 고치는 법

목록에 **줄 하나 추가**하는 게 전부다. 예를 들어 `certData.ts`에 자격증을 더하려면

```ts
{id: 'c41', n:"방수기능사", d:12.0, p:15.0, dens:3200},
```

이렇게 한 줄 넣고 저장하면 화면에 나온다. 형식(따옴표, 쉼표)만 옆줄이랑 똑같이 맞추면 된다.

---

## ⚠️ certData.ts 관련 — 정하고 가야 할 것

두 가지 문제가 있다.

**1. 지금 아무 데서도 안 쓰인다.** `certRawData`를 불러다 쓰는 화면이 없다. 만들다 만 상태다.

**2. 숫자에 출처가 없다.** `d`, `p`, `dens`가 각각 뭔지 파일에 안 적혀 있다.
이 프로젝트 원칙이 "가짜 수치 금지 · 실측과 출처만"인데 여기가 비어 있다.

**대안이 있다.** `certgap`(별도 저장소)이 Q-Net 공공데이터에서 **연령별 실제 취득자 수**를
받아온다. 출처가 명확하고 상업적 이용도 가능하다.

셋 중 하나를 골라야 한다.

- (가) `certData.ts`의 숫자 출처를 찾아서 파일에 적는다
- (나) `certgap`이 뽑은 Q-Net 숫자로 갈아끼운다
- (다) 이 파일을 지운다 (안 쓰이므로 지워도 화면에 영향 없음)

**정하기 전까지는 이 파일에 자격증을 더하지 않는다.** 출처 없는 숫자가 늘어나기만 한다.
